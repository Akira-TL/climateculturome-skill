# Quickstart

1. 准备 `request.json`。
2. 指向运行时数据文件。
3. 执行 `preflight`。
4. 根据 capability gate 决定是仅做描述性分析，还是进入样本级统计。
5. 构造环境背景。
6. 分析微生物结构。
7. 生成候选关联与假说。
8. 人工审核。
9. 输出 provenance / claim ledger / audit。

核心原则：**数据不够时，降级分析，而不是补结果。**
